#include <QApplication>
#include <QLabel>
#include <QWidget>
int main(int argc, char *argv[ ])
{
    QApplication app(argc, argv);
    QLabel hello("<center>Urko se descarga porno afgano everyday</center>");
    hello.setWindowTitle("Fucking windows tittle");
    hello.resize(400, 400);
    hello.show();
    return app.exec();
}
